# Migration `20200709114332-add-vote-model`

This migration has been generated by David Salami at 7/9/2020, 11:43:33 AM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
PRAGMA foreign_keys=OFF;

CREATE TABLE "Vote" (
"createdAt" DATE NOT NULL DEFAULT CURRENT_TIMESTAMP,
"id" INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
"linkId" INTEGER NOT NULL,
"userId" INTEGER NOT NULL,FOREIGN KEY ("linkId") REFERENCES "Link"("id") ON DELETE CASCADE ON UPDATE CASCADE,
FOREIGN KEY ("userId") REFERENCES "User"("id") ON DELETE CASCADE ON UPDATE CASCADE)

CREATE UNIQUE INDEX "Vote.linkId_userId" ON "Vote"("linkId","userId")

PRAGMA foreign_key_check;

PRAGMA foreign_keys=ON;
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200708172208-add-user-model..20200709114332-add-vote-model
--- datamodel.dml
+++ datamodel.dml
@@ -1,8 +1,8 @@
 // 1
 datasource db {
   provider = "sqlite"
-  url = "***"
+  url = "***"
 }
 // 2
 generator client {
@@ -15,16 +15,29 @@
   createdAt   DateTime @default(now())
   description String
   url         String
   postedBy    User?    @relation(fields: [postedById], references: [id])
-  postedById      Int?      
+  postedById  Int?
+  votes       Vote[]
 }
 model User {
-  id          Int       @id @default(autoincrement())
-  createdAt   DateTime  @default(now())
-  firstName   String    
-  lastName    String
-  email       String    @unique
-  password    String
-  links       Link[]
+  id        Int      @id @default(autoincrement())
+  createdAt DateTime @default(now())
+  firstName String
+  lastName  String
+  email     String   @unique
+  password  String
+  links     Link[]
+  votes     Vote[]
 }
+
+model Vote {
+  id        Int      @id @default(autoincrement())
+  createdAt DateTime @default(now())
+  link      Link     @relation(fields: [linkId], references: [id])
+  linkId    Int
+  userId    Int
+  user      User     @relation(fields: [userId], references: [id])
+
+  @@unique([linkId, userId])
+}
```


